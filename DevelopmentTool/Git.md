# Git
## Commands
```
gti remote -v
git remote add
git remote remove 
git clone
git add .
git commit -m "comment"
git push REPO BRANCH
git pull REPO BRANCH
git checkout -b NEW_BRANCH
git checkout BRANCH
```